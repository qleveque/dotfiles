#!/bin/bash
[[ $1 == '-s' ]] && cmd='split-pane --bottom' && shift || cmd='spawn'
is_windows=$(grep -q Microsoft /proc/version)
${is_windows} && cwd="$(wslpath -m ${PWD})" || cwd="${PWD}"
wezterm cli ${cmd} --cwd ${cwd}  -- zsh -c "$@"
