#!/bin/bash
[[ $1 == '-s' ]] && cmd='split-pane --bottom' && shift || cmd='spawn'
if grep -q Microsoft /proc/version; then
  WEZTERM="wezterm.exe" cwd="$(wslpath -m ${PWD})"
else
  WEZTERM="wezterm" cwd="${PWD}"
fi
${WEZTERM} cli ${cmd} --cwd ${cwd}  -- zsh -c "$@"
