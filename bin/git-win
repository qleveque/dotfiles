#!zsh

if [[ $PWD == /mnt/c/* ]]; then
  git.exe \
    -c core.editor='ce(){ wsl.exe zsh -c "nvim \`wslpath $1\`"; clear; }; ce' \
    -c core.pager='cp(){ wsl.exe zsh -c "less"; }; cp' \
    "$@"
else
  /run/current-system/sw/bin/git "$@"
fi
